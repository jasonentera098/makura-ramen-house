<UserControl x:Class="IT_Helpdesk.Views.TicketUpdateContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IT_Helpdesk.Views"
             xmlns:converters="clr-namespace:IT_Helpdesk.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             FontFamily="Roboto">

    <UserControl.Resources>
        <!-- Converters -->
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        
        <!-- Read-only input style -->
        <Style x:Key="ReadOnlyInputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="#F9F9F9"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Read-only multiline text style -->
        <Style x:Key="ReadOnlyTextAreaStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Background" Value="#F9F9F9"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Editable text area style -->
        <Style x:Key="FormTextAreaStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox style -->
        <Style x:Key="FormComboStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Browse button style -->
        <Style x:Key="BrowseButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Submit button style -->
        <Style x:Key="SubmitButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Field label style -->
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#444444"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Border Background="White"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="32"
                    MaxWidth="700"
                    HorizontalAlignment="Left"
                    Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.08"/>
                </Border.Effect>

                <StackPanel>
                    <!-- Form Title -->
                    <TextBlock Text="Update Ticket"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="#1A1A2E"
                               Margin="0,0,0,8"/>
                    
                    <TextBlock Text="Update the ticket status and add remarks about your progress."
                               FontSize="13"
                               Foreground="#666666"
                               Margin="0,0,0,24"/>

                    <!-- Title Field (Read-Only) -->
                    <TextBlock Text="Title" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox x:Name="txtTitle"
                             Text="{Binding Title, Mode=OneWay}"
                             Style="{StaticResource ReadOnlyInputStyle}"/>

                    <!-- Description Field (Read-Only) -->
                    <TextBlock Text="Description" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox x:Name="txtDescription"
                             Text="{Binding Description, Mode=OneWay}"
                             Style="{StaticResource ReadOnlyTextAreaStyle}"
                             Height="100"/>

                    <!-- Status Field (ComboBox) -->
                    <TextBlock Text="Status *" Style="{StaticResource FieldLabelStyle}"/>
                    <ComboBox x:Name="cmbStatus"
                              ItemsSource="{Binding AvailableStatuses}"
                              SelectedItem="{Binding SelectedStatus, Mode=TwoWay}"
                              Style="{StaticResource FormComboStyle}"/>

                    <!-- Remarks Field (TextBox) -->
                    <TextBlock Text="Remarks *" Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox x:Name="txtRemarks"
                             Text="{Binding Remarks, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FormTextAreaStyle}"
                             Height="120"
                             MaxLength="2000"
                             ToolTip="Add your troubleshooting notes, findings, or resolution details"/>

                    <!-- Attachment Field (Optional) -->
                    <TextBlock Text="Attachment (Optional)" Style="{StaticResource FieldLabelStyle}"/>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBox x:Name="txtAttachmentPath"
                                 Grid.Column="0"
                                 Text="{Binding AttachmentPath, Mode=TwoWay}"
                                 IsReadOnly="True"
                                 Height="36"
                                 Padding="10,0"
                                 VerticalContentAlignment="Center"
                                 FontSize="13"
                                 BorderBrush="#D0D0D0"
                                 BorderThickness="1"
                                 Background="#F9F9F9"
                                 Margin="0,0,8,0">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="TextBox">
                                                <Border Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        CornerRadius="4,0,0,4">
                                                    <ScrollViewer x:Name="PART_ContentHost"
                                                                  Margin="{TemplateBinding Padding}"
                                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                        
                        <Button x:Name="btnBrowse"
                                Grid.Column="1"
                                Content="Browse..."
                                Width="100"
                                Style="{StaticResource BrowseButtonStyle}"
                                Click="btnBrowse_Click"/>
                    </Grid>

                    <!-- Error Message -->
                    <TextBlock x:Name="txtError"
                               Text="{Binding ErrorMessage}"
                               FontSize="12"
                               Foreground="#D32F2F"
                               TextWrapping="Wrap"
                               Margin="0,0,0,16">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ErrorMessage}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ErrorMessage}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                                <Setter Property="Visibility" Value="Visible"/>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Submit Button -->
                    <Button x:Name="btnSubmit"
                            Content="Update Ticket"
                            Width="160"
                            HorizontalAlignment="Left"
                            Style="{StaticResource SubmitButtonStyle}"
                            Command="{Binding UpdateCommand}"
                            IsEnabled="{Binding IsUpdating, Converter={StaticResource InverseBooleanConverter}}"
                            Margin="0,8,0,0"/>

                    <!-- Existing Attachments Section -->
                    <TextBlock Text="Existing Attachments"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#1A1A2E"
                               Margin="0,32,0,12"/>

                    <!-- Empty attachments message -->
                    <TextBlock Text="No attachments for this ticket."
                               FontSize="13"
                               Foreground="#AAAAAA"
                               Margin="0,0,0,8">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Attachments.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Attachments ItemsControl -->
                    <ItemsControl ItemsSource="{Binding Attachments}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8F9FA"
                                        BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="14,10"
                                        Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding FileName}"
                                                           FontSize="13"
                                                           FontWeight="SemiBold"
                                                           Foreground="#333333"/>
                                                <!-- Unavailability indicator shown only when file is missing -->
                                                <TextBlock Text="{Binding AvailabilityLabel}"
                                                           FontSize="12"
                                                           Foreground="#D32F2F"
                                                           Margin="6,0,0,0"
                                                           VerticalAlignment="Center">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding FileAvailable}" Value="False">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </StackPanel>
                                            <TextBlock Text="{Binding UploadedAt, StringFormat='Uploaded: {0:MMM dd, yyyy  hh:mm tt}'}"
                                                       FontSize="11"
                                                       Foreground="#888888"
                                                       Margin="0,2,0,0"/>
                                        </StackPanel>

                                        <!-- Open button — disabled when file is unavailable -->
                                        <Button Grid.Column="1"
                                                Content="Open"
                                                Width="70"
                                                Height="30"
                                                FontSize="12"
                                                Cursor="Hand"
                                                IsEnabled="{Binding FileAvailable}"
                                                Command="{Binding DataContext.OpenAttachmentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"
                                                Background="#007ACC"
                                                Foreground="White"
                                                BorderThickness="0">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        CornerRadius="4"
                                                                        Padding="8,4">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Opacity" Value="0.85"/>
                                                                    </Trigger>
                                                                    <Trigger Property="IsEnabled" Value="False">
                                                                        <Setter Property="Opacity" Value="0.4"/>
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Update History Section -->
                    <TextBlock Text="Update History"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#1A1A2E"
                               Margin="0,32,0,12"/>

                    <!-- Empty history message -->
                    <TextBlock Text="No updates recorded yet."
                               FontSize="13"
                               Foreground="#AAAAAA"
                               Margin="0,0,0,8">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding UpdateHistory.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- History ItemsControl -->
                    <ItemsControl ItemsSource="{Binding UpdateHistory}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8F9FA"
                                        BorderBrush="#E0E0E0"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="14,10"
                                        Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Row 0: Date + Updated By -->
                                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
                                            <TextBlock Text="{Binding UpdateDate, StringFormat='{}{0:MMM dd, yyyy  hh:mm tt}'}"
                                                       FontSize="12"
                                                       FontWeight="SemiBold"
                                                       Foreground="#555555"/>
                                            <TextBlock Text=" — "
                                                       FontSize="12"
                                                       Foreground="#888888"/>
                                            <TextBlock Text="{Binding UpdatedByName}"
                                                       FontSize="12"
                                                       FontWeight="SemiBold"
                                                       Foreground="#007ACC"/>
                                        </StackPanel>

                                        <!-- Row 1: Status Change (only if present) -->
                                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,4">
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding StatusChange}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding StatusChange}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <TextBlock Text="Status: "
                                                       FontSize="12"
                                                       Foreground="#666666"/>
                                            <TextBlock Text="{Binding StatusChange}"
                                                       FontSize="12"
                                                       FontWeight="SemiBold"
                                                       Foreground="#28A745"/>
                                        </StackPanel>

                                        <!-- Row 2: Remarks -->
                                        <TextBlock Grid.Row="2"
                                                   Text="{Binding Remarks}"
                                                   FontSize="13"
                                                   Foreground="#333333"
                                                   TextWrapping="Wrap"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
